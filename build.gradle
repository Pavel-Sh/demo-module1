plugins {
	id 'java-library'
	id 'maven-publish'
}

group = 'com.pavel'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '17'

repositories {
	mavenCentral()
	mavenLocal()
}

dependencies {
}

publishing {
	publications {
		mavenJava(MavenPublication) {

			version = '0.0.2'
			from components.java
		}
	}
	repositories {
		maven {
			url 'https://test-748392735374.d.codeartifact.us-west-2.amazonaws.com/maven/test/'
			credentials {
				username "aws"
				password System.env.CODEARTIFACT_AUTH_TOKEN
			}
		}
	}
}
